{
  "name": "high-performance-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "package": "turbo run package"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "turbo": "^1.10.0",
    "typescript": "^5.0.0"
  }
}
