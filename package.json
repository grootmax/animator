{
  "name": "high-performance-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "package": "turbo run package"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "ts-node": "^10.9.2",
    "turbo": "^1.10.0",
    "typescript": "^5.0.0",
    "vitest": "^4.1.10"
  }
}
